{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install && cd webview-ui && npm install",
  "buildCommand": "cd webview-ui && npm run build",
  "outputDirectory": "dist/webview",
  "framework": null,
  "functions": {
    "api/agents.ts": {
      "maxDuration": 15
    }
  }
}
